<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.unknowcabbes.recodoch">

    <application
        android:allowBackup="true"
        android:background="@color/colorBackgroundScreen"
        android:icon="@mipmap/recodoch_logo"
        android:label="Recodoch"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoActionBar">
        <activity android:name=".HomeActivity"/>
        <activity android:name=".forms.FormularioTipoDocumentoActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".forms.FormularioTipoUsuarioActivity" />
        <activity android:name=".forms.FormularioClienteComplementarActivity" />
        <activity android:name=".forms.FormularioClienteActivity" />
        <activity android:name=".forms.FormularioUsuarioActivity" />
        <activity android:name=".forms.FormularioDocumentoActivity" />
        <activity android:name=".VisualizaDocumentosActivity">
        </activity>
    </application>

</manifest>